\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{sty/numerique}[2022/01/28 numerique]

\RequirePackage{frenchmath} % Modifie l'affichage des mathématiques pour l'adapter aux conventions françaises
\RequirePackage{amsmath,amssymb,amsfonts}
\RequirePackage{eurosym}
\RequirePackage[locale=FR, group-digits=all, group-separator=\ , group-minimum-digits=4]{siunitx} % Pour afficher les valeurs numériques avec unités correctement
\DeclareSIUnit{\cube}{\cubed}
\DeclareSIUnit{\carre}{\squared}
\DeclareSIUnit{\litre}{\ell}
\DeclareSIUnit{\EURO}{\text{\euro}}


% Ensemble de nombres
\newcommand{\ensembleNombres}[1]{\ensuremath{\mathbb{#1}}}
\newcommand{\N}{\ensembleNombres{N}}
\newcommand{\Z}{\ensembleNombres{Z}}
\newcommand{\D}{\ensembleNombres{D}}
\newcommand{\Q}{\ensembleNombres{Q}}
\newcommand{\R}{\ensembleNombres{R}}
\newcommand{\C}{\ensembleNombres{C}}

% Redéfini le symbole racine carrée pour que le symbole se referme sur la fin
\let\oldsqrt\sqrt
\def\sqrt{\mathpalette\DHLhksqrt}
\def\DHLhksqrt#1#2{%
\setbox0=\hbox{$#1\oldsqrt{#2\,}$}\dimen0=\ht0
\advance\dimen0-0.2\ht0
\setbox2=\hbox{\vrule height\ht0 depth -\dimen0}%
{\box0\lower0.4pt\box2}}